# Generated by Ansible - Pi-hole v6 TOML
# Adjust via group_vars/host_vars overrides if needed.

[server]
# Interface Pi-hole listens on. Adjust if not eth0.
interface = "eth0"
blocking_enabled = true

[dns]
# Enable dnsmasq config directory loading for local DNS records
etc_dnsmasq_d = true

[webui]
# Admin password for API authentication
{% if pihole_admin_password is defined and pihole_admin_password %}
admin.password = "{{ pihole_admin_password }}"
{% endif %}

# Additional configuration from playbook variables
{{ pihole_toml_extra | default('') }}
