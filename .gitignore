### TERRAFORM ####
# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data, such as
# password, private keys, and other secrets. These should not be part of version
# control as they are data points which are potentially sensitive and subject
# to change depending on the environment.
*.tfvars
*.tfvars.json

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Include override files you do wish to add to version control using negated pattern
# !example_override.tf

# Include tfplan files to ignore the plan output of command: terraform plan -out=tfplan
# example: *tfplan*

# Ignore CLI configuration files
.terraformrc
terraform.rc

#### ANSIBLE ####
*.retry

## Ignore the python virtual environment
ansible_env/
.ansible
.venv/
venv/

# generated files
node_exporter_hosts.json

# tmp / log files
tmp/

# config files for servers, ignore for now
config/

# archive folder
archive/

# TODO file
TODO

# vault files
.vaultpass
secret
*.secret
secret.yml

# TODO files - personal notes
TODO
TODO-*
TODO_*

# Ignore unencrypted secrets
**/secrets.yml
# Don't ignore encrypted secrets
!*.encrypted.yml

# ISO files
*.iso
packer/proxmox/downloaded_iso_path/tools/ipxe/
# Ignore Packer ISO lock files
*.iso.lock
tools/ipxe/

# Add to .gitignore
packer/proxmox/secrets.yml
packer/proxmox/*/secrets.yml
credentials.pkr.hcl

# Credential files and folders
credentials/
**/credentials/
credentials
**/credentials
*.credentials
credential.yml
credentials.yml
**/credential.yml
**/credentials.yml

# API tokens and keys
*.ini
!**/requirements.ini
proxmox_api_token*
*api_token*
*_token*
*.key
*.pem


## Packer Ignore
# Packer
packer_cache/
*.pid
.packer.log

# Secret vars
variables/secrets.pkrvars.hcl

# VS Code
.vscode/

ansible/credentials

# Packer generated files
packer/**/http/user-data
