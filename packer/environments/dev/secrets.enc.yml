# Staging environment secrets
environments:
  stage:
    pve1:
      api_url: "https://stage-pve1.flopo.retropetro.net:8006/api2/json"
      api_token_id: "terraform-user@pve!tfe-token"
      api_token_secret: "stage-secret"
      node: "pve1"
      storage_pools:
        local: "local"
        zfs: "zfsdata01"

passwords:
  truenas_root: "StagePass123"
  opnsense_root: "StagePass123"
  ubuntu_user: "pofo14"

global:
  network_bridge: "vmbr0"
  network_model: "virtio"
  insecure_skip_tls_verify: true
  task_timeout: "30m"
